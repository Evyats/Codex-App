/** @jsxImportSource nativewind */
import { cssInterop } from 'react-native-css-interop';
import {
  Badge as PaperBadge,
  Button as PaperButton,
  Card as PaperCard,
  Checkbox as PaperCheckbox,
  Chip as PaperChip,
  Divider as PaperDivider,
  IconButton as PaperIconButton,
  ProgressBar as PaperProgressBar,
  RadioButton as PaperRadioButton,
  SegmentedButtons as PaperSegmentedButtons,
  Surface as PaperSurface,
  Switch as PaperSwitch,
  Text as PaperText,
  TextInput as PaperTextInput,
} from 'react-native-paper';

export const Badge = cssInterop(PaperBadge, { className: 'style' });
export const Button = cssInterop(PaperButton, { className: 'style' });
export const Card = cssInterop(PaperCard, { className: 'style' });
export const CardActions = cssInterop(PaperCard.Actions, { className: 'style' });
export const CardContent = cssInterop(PaperCard.Content, { className: 'style' });
export const CardTitle = cssInterop(PaperCard.Title, { className: 'style' });
export const Checkbox = cssInterop(PaperCheckbox, { className: 'style' });
export const Chip = cssInterop(PaperChip, { className: 'style' });
export const Divider = cssInterop(PaperDivider, { className: 'style' });
export const IconButton = cssInterop(PaperIconButton, { className: 'style' });
export const ProgressBar = cssInterop(PaperProgressBar, { className: 'style' });
export const RadioButton = cssInterop(PaperRadioButton, { className: 'style' });
export const SegmentedButtons = cssInterop(PaperSegmentedButtons, { className: 'style' });
export const Surface = cssInterop(PaperSurface, { className: 'style' });
export const Switch = cssInterop(PaperSwitch, { className: 'style' });
export const Text = cssInterop(PaperText, { className: 'style' });
export const TextInput = cssInterop(PaperTextInput, { className: 'style' });
